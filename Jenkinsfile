pipeline {
  agent any

  stages {

    stages {

      stage('Compile') {
        steps {
          echo 'Compile'
        }
      }

      stage('Test Cases') {
        steps {
          echo 'Test Cases'
        }
      }

      stage('Docker Build') {
        steps {
          echo 'Docker Build'
        }
      }

      stage('Docker Push') {
        steps {
          echo 'Docker Push'
        }
      }

      stage('Deploy to Dev') {
        steps {
          echo 'Deploy to Dev'
        }
      }

    }

  }

}

